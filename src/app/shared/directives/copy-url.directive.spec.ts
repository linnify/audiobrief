import { CopyUrlDirective } from './copy-url.directive';

describe('CopyUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyUrlDirective();
    expect(directive).toBeTruthy();
  });
});
